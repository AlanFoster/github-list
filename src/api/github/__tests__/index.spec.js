import * as service from '../';

describe('github-api', function () {
  describe('fetchTopJavaScriptRepositories', function () {
    // Ensure mapped response
    // Ensure callbacks with jest

    it('has a placeholder', function () {

    });
  });
});
